{
  "name": "travacs-rules-tests",
  "description": "Firestore Security Rules tests (emulator).",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "mocha test/firestore.test.js --timeout 20000"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "mocha": "^10.8.2"
  }
}
