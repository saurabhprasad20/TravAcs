# Local env / secrets
.env
.env.local

# Firebase per-project native config (generated by `flutterfire configure`)
app/android/app/google-services.json
app/ios/Runner/GoogleService-Info.plist
.firebase/
firebase/functions/node_modules/

# Built artifacts / build logs (don't commit binaries)
*.apk
app/build_apk*.log

# One-off local admin helper (reads your firebase login; not for the repo)
enable_phone.js
